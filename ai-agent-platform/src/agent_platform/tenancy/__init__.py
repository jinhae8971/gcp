"""Multi-tenancy primitives (Phase 5).

Modules:
- tenant: Tenant registry and tenant-scoped session store wrapper
- team_config: Per-team prompt/model/harness overrides
- usage_report: Aggregated usage metrics per tenant/project
"""
